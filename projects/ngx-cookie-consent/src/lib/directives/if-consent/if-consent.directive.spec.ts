import { IfConsentDirective } from './if-consent.directive';

describe('IfConsentDirective', () => {
  it('should create an instance', () => {
    const directive = new IfConsentDirective();
    expect(directive).toBeTruthy();
  });
});
